# Branch Conditional If Expr Release Harness Delta Proof Pressure v0

Card: S3-R195-C2-X
Agent: [Igniter-Lang External Pressure Reviewer]
Role: external-pressure-reviewer
Mode: discussion
Initiator: architect-supervisor
Track: branch-conditional-if-expr-release-harness-delta-proof-pressure-v0

Context: internal — full read access to C1-I proof track doc, delta summary JSON,
proof runner source, R194 C1-A authorization review, R194 C2-S status curation,
and live SHA256 of historical harness summary file
Write access: none
Canon authority: none

---

## Question

Does the S3-R195-C1-I `if_expr` release-harness delta proof correctly use
`evidence_label: if_expr_internal_compiler_delta` and
`evidence_class: post_alpha_compiler_only_delta`, avoid all forbidden labels,
cover D-1..D-13 with passing results, keep old release evidence files unchanged
(verified by live SHA256), preserve the historical `branch_conditional_if_expr`
exclusion without rewriting it, exclude runtime/evaluator from both invocation
and claim, keep public/demo/stable/production/all-grammar/Spark/API/CLI claims
closed, make no TypeChecker/SemanticIR/compiler behavior changes, and stay inside
the C1-A-authorized write scope?

---

## Inputs Read

- `igniter-lang/docs/tracks/branch-conditional-if-expr-release-harness-delta-proof-v0.md` (S3-R195-C1-I)
- `igniter-lang/docs/tracks/stage3-round194-status-curation-v0.md` (S3-R194-C2-S)
- `igniter-lang/docs/tracks/branch-conditional-if-expr-release-harness-delta-authorization-review-v0.md` (S3-R194-C1-A)
- `igniter-lang/experiments/branch_conditional_if_expr_release_harness_delta_v0/out/branch_conditional_if_expr_release_harness_delta_summary.json` (generated by C1-I)
- `igniter-lang/experiments/branch_conditional_if_expr_release_harness_delta_v0/branch_conditional_if_expr_release_harness_delta_v0.rb` (runner source, fully read)
- Live SHA256 of `experiments/compiler_release_acceptance_harness_v0/out/compiler_release_acceptance_harness_summary.json` (independently computed)

---

## Direct JSON Gate Verification

All C1-A required summary fields verified directly from the delta summary JSON:

```text
evidence_label:   if_expr_internal_compiler_delta  ✓
evidence_class:   post_alpha_compiler_only_delta    ✓
status:           PASS                              ✓
checks_total:     39                                ✓
checks_pass:      39                                ✓
checks_fail:      0                                 ✓
failed_checks:    []                                ✓

release_scope.scope:                        if_expr_internal_compiler_delta  ✓
release_scope.claimed_surfaces:             [typechecker_if_expr_v0, typed_semanticir_if_expr_v0]  ✓
release_scope.excluded_surfaces:            [runtime_evaluator, lazy_branch_execution,
                                             public_api_cli_widening, spark, public_demo,
                                             stable, production, all_grammar]  ✓
release_scope.old_evidence_rewritten:       false  ✓
release_scope.public_claims_authorized:     false  ✓
release_scope.production_runtime_authorized: false  ✓

old_evidence_immutability.compiler_release_acceptance_harness_summary_unchanged: true  ✓
old_evidence_immutability.official_first_rc_evidence_summary_unchanged:          true  ✓
old_evidence_immutability.combined_post_prep_smoke_summary_unchanged:            true  ✓
old_evidence_immutability.harness_summary_sha256_matched:                        true  ✓
old_evidence_immutability.harness_historical_exclusion:   ["branch_conditional_if_expr"]  ✓
old_evidence_immutability.first_rc_historical_exclusion:  ["branch_conditional_if_expr"]  ✓
old_evidence_immutability.exclusion_basis:  "S3-R164-C4-A Portfolio acceptance of ..."  ✓

non_claims.not_official_first_rc_evidence:          true  ✓
non_claims.not_alpha_release_evidence:              true  ✓
non_claims.not_release_execution_evidence:          true  ✓
non_claims.no_release_execution:                    true  ✓
non_claims.no_public_demo_claim:                    true  ✓
non_claims.no_stable_production_all_grammar_claim:  true  ✓
non_claims.no_runtime_evaluator_support:            true  ✓
non_claims.no_spark_claim:                          true  ✓
non_claims.no_public_api_cli_widening:              true  ✓
non_claims.no_typechecker_semanticir_behavior_change: true  ✓

closed_surface_scan.parser_not_in_write_scope:              true  ✓
closed_surface_scan.release_harness_not_in_write_scope:     true  ✓
closed_surface_scan.first_rc_evidence_not_in_write_scope:   true  ✓
closed_surface_scan.smoke_summary_not_in_write_scope:       true  ✓
closed_surface_scan.typechecker_not_in_write_scope:         true  ✓
closed_surface_scan.semanticir_emitter_not_in_write_scope:  true  ✓
closed_surface_scan.docs_spec_not_in_write_scope:           true  ✓
closed_surface_scan.runtime_not_loaded:                     true  ✓
closed_surface_scan.status:                                 PASS  ✓
```

SHA256 immutability anchor — independently verified live:

```text
Expected (KNOWN_HARNESS_SUMMARY_SHA256 in runner):
  bc8d69f65c9267a604cb47e8ce0498a8373a80eaa264a2c53892139552a2618b

Actual (computed from live file):
  bc8d69f65c9267a604cb47e8ce0498a8373a80eaa264a2c53892139552a2618b

Match: YES  ✓
```

Derivative OOF-TY0 secondary classification — verified per negative case:

```text
non_bool_condition:     primary=[OOF-IF1]  secondary=[]                  oof_ty0_absent=true  ✓
missing_else:           primary=[OOF-IF2]  secondary=[OOF-TY0 sec]       oof_ty0_absent=true  ✓
branch_type_mismatch:   primary=[OOF-IF3]  secondary=[OOF-TY0 sec]       oof_ty0_absent=true  ✓
empty_branch:           primary=[OOF-IF4]  secondary=[OOF-TY0 sec]       oof_ty0_absent=true  ✓
```

Each secondary `OOF-TY0` entry carries:
```json
{
  "classification": "secondary_type_propagation",
  "secondary_type_propagation": true,
  "unsupported_if_expr_regression": false
}
```

---

## Scope Check Matrix

| ID | Check | Evidence | Result |
| --- | --- | --- | --- |
| SC-1 | `evidence_label: if_expr_internal_compiler_delta` | `summary.evidence_label == "if_expr_internal_compiler_delta"`; C1-A required value exact match | PASS |
| SC-2 | `evidence_class: post_alpha_compiler_only_delta` | `summary.evidence_class == "post_alpha_compiler_only_delta"`; C1-A allowed class exact match | PASS |
| SC-3 | No forbidden labels — not official first-RC, alpha, release, public demo, runtime, production, Spark, or all-grammar evidence | All 8 forbidden labels absent from summary; all 10 `non_claims` fields true; `release_scope.public_claims_authorized: false`; track doc wording confirms outputs may be called only `if_expr_internal_compiler_delta evidence` | PASS |
| SC-4 | D-1..D-13 all present and passing | 39 sub-checks across all 13 D-items; `checks_total: 39`, `checks_pass: 39`, `checks_fail: 0`; `failed_checks: []`; all check name prefixes D-1 through D-13 present in checks array | PASS |
| SC-5 | Old release evidence files unchanged | Live SHA256 of harness summary matches KNOWN anchor exactly; `harness_summary_sha256_matched: true`; all 3 `old_evidence_immutability` unchanged booleans true; runner reads files at known paths and never opens them for writing | PASS |
| SC-6 | Historical `branch_conditional_if_expr` exclusion not rewritten | `harness_historical_exclusion: ["branch_conditional_if_expr"]` and `first_rc_historical_exclusion: ["branch_conditional_if_expr"]` are read-only from old files; `exclusion_basis: "S3-R164-C4-A..."` preserved; `old_evidence_rewritten: false`; delta packet adds new post-alpha evidence and does not modify historical exclusion fields | PASS |
| SC-7 | Runtime/evaluator not invoked and not claimed | Runner requires only `parser`, `classifier`, `typechecker`, `semanticir_emitter`; D-11 verifies `$LOADED_FEATURES` contains no `runtime`, `evaluator`, `runtime_machine`, or `lazy_branch` module; `closed_surface_scan.runtime_not_loaded: true`; `no_runtime_evaluator_support: true`; `production_runtime_authorized: false` | PASS |
| SC-8 | Public/demo/stable/production/all-grammar claims closed | `no_public_demo_claim: true`; `no_stable_production_all_grammar_claim: true`; `public_claims_authorized: false`; `production_runtime_authorized: false`; all forbidden label booleans explicitly true in `non_claims` | PASS |
| SC-9 | Spark/API/CLI closed | `no_spark_claim: true`; `no_public_api_cli_widening: true`; `release_scope.excluded_surfaces` contains both `spark` and `public_api_cli_widening` | PASS |
| SC-10 | TypeChecker/SemanticIR/compiler behavior not changed | `no_typechecker_semanticir_behavior_change: true`; D-13 verifies `typechecker_not_in_write_scope: true` and `semanticir_emitter_not_in_write_scope: true`; C1-I changed-file list contains only runner, summary JSON, and track doc — no lib/ files | PASS |
| SC-11 | Write scope stayed inside C1-A boundary | C1-A authorized scope: `experiments/branch_conditional_if_expr_release_harness_delta_v0/**` and `docs/tracks/branch-conditional-if-expr-release-harness-delta-proof-v0.md`; C1-I changed 3 files: runner (within experiment dir), summary JSON (within experiment/out/ dir), track doc (exact authorized path); no other files changed | PASS |

Overall: **11/11 PASS** — no blockers.

---

## D-Item Coverage Detail

D-1 through D-13 verified against the required matrix from C1-A:

| D-item | Sub-checks | Summary |
| --- | --- | --- |
| D-1 | 3 | Minimal if_else: no type errors, SemanticIR present, typed if_expr shape with cond/then/else keys and resolved_type Integer |
| D-2 | 3 | Nested if_else: no type errors, SemanticIR present, resolved_type Integer |
| D-3 | 2 | non_bool_condition: OOF-IF1 present, unsupported-if_expr OOF-TY0 absent |
| D-4 | 2 | missing_else: OOF-IF2 present, unsupported-if_expr OOF-TY0 absent |
| D-5 | 2 | branch_type_mismatch: OOF-IF3 present, unsupported-if_expr OOF-TY0 absent |
| D-6 | 2 | empty_branch: OOF-IF4 present, unsupported-if_expr OOF-TY0 absent |
| D-7 | 1 | OOF-IF5 absent across all 6 fixtures |
| D-8 | 1 | Unsupported-if_expr OOF-TY0 absent across all 6 fixtures |
| D-9 | 1 | Derivative OOF-TY0 labeled `secondary_type_propagation` where present |
| D-10 | 5 | SemanticIR flat keys (condition/then_branch/else_branch/resolved_type), no branch wrapper, nested inner flat keys, resolved_type preserved |
| D-11 | 2 | Runtime/evaluator not loaded, only 4 compiler modules loaded |
| D-12 | 7 | Harness PASS + exclusion + SHA256 match; first-RC PASS + label + exclusion; smoke PASS + no branch conditional claim |
| D-13 | 7 | Parser/release harness/first-RC evidence/smoke/typechecker/semanticir emitter/docs spec all outside write scope |

Total: 39 sub-checks, all PASS.

---

## [Agree]

- The `evidence_label` and `evidence_class` values exactly match the C1-A
  required strings. The summary is correctly identified as
  `if_expr_internal_compiler_delta` / `post_alpha_compiler_only_delta`, not
  as official first-RC, alpha, release, or public demo evidence.

- All 10 C1-A required `non_claims` fields are present and set to `true`.
  The complete set — including `not_official_first_rc_evidence`,
  `not_alpha_release_evidence`, `not_release_execution_evidence`,
  `no_release_execution`, `no_public_demo_claim`,
  `no_stable_production_all_grammar_claim`, `no_runtime_evaluator_support`,
  `no_spark_claim`, `no_public_api_cli_widening`, and
  `no_typechecker_semanticir_behavior_change` — covers every forbidden
  label and surface from C1-A.

- D-1..D-13 are all present and pass. The 39-sub-check matrix is a
  substantial extension of the required minimum 13 D-items, with the
  additional sub-checks providing stronger evidence without widening scope.

- The SHA256 anchor for the historical harness summary is independently
  confirmed: live `shasum -a 256` of the file returns
  `bc8d69f65c9267a604cb47e8ce0498a8373a80eaa264a2c53892139552a2618b`,
  matching `KNOWN_HARNESS_SUMMARY_SHA256` exactly. The D-12 SHA256 check
  is therefore a real immutability assertion, not a vacuous self-comparison.

- The historical `branch_conditional_if_expr` exclusion is preserved
  without modification. The runner reads `excluded_features` and
  `exclusion_basis` from the old files and places them as read-only fields
  in `old_evidence_immutability`. It does not write to any historical
  evidence path.

- Runtime/evaluator is excluded at both the loading level and the claim
  level. D-11 uses `$LOADED_FEATURES` to machine-assert that no
  `runtime`, `evaluator`, `runtime_machine`, or `lazy_branch` module was
  loaded during the proof run. The `require` list at the top of the runner
  is limited to the 4 compiler pipeline modules.

- The `secondary_oof_ty0_errors` helper in the runner correctly implements
  the R193 hygiene classification pattern. It selects `OOF-TY0` entries
  whose messages do not include `Unsupported expression kind: if_expr` and
  labels them `secondary_type_propagation: true`,
  `unsupported_if_expr_regression: false`. This pattern is mechanically
  identical to the R193 hygiene baseline, preventing the R190 OOF-TY0
  ambiguity from appearing in the new delta evidence.

- The `release_scope.claimed_surfaces` is exactly
  `[typechecker_if_expr_v0, typed_semanticir_if_expr_v0]` — the two
  compiler-only surfaces authorized by C1-A. All 8 excluded surfaces
  are present in `release_scope.excluded_surfaces`.

- The C1-A minimum required summary shape is fully satisfied: all
  required top-level fields present, `release_scope` complete with
  all required sub-fields, `old_evidence_immutability` complete with
  all 3 required booleans, and `non_claims` complete with all 10
  required fields.

- Write scope discipline is correct. The 3 changed files (runner,
  summary JSON, track doc) map to the 2 authorized write paths in C1-A
  without overlap onto any closed surface.

---

## [Challenge]

No blocking challenges.

One structural observation on `closed_surface_scan.authorized_write_paths`
(see NB-1).

---

## [Missing]

No blocking gaps. One cosmetic non-blocking note:

**NB-1: `closed_surface_scan.authorized_write_paths` records runner-local write scope, not the full C1-A card write scope**

The runner's `DELTA_AUTHORIZED_WRITE_PATHS` constant contains only
`branch_conditional_if_expr_release_harness_delta_v0/` and its `out/`
subdirectory. It does not include
`docs/tracks/branch-conditional-if-expr-release-harness-delta-proof-v0.md`,
which is the second path in the C1-A-authorized write scope.

This is architecturally correct: the track doc is a manually-written
markdown file that the runner cannot write to. The D-13 checks verify
that the runner's own write scope excludes closed paths — the track doc
is not a closed surface and does not appear in D-13 checks.

A future reader comparing C1-A authorized paths with
`closed_surface_scan.authorized_write_paths` might note the track doc
absence and wonder whether it was in scope. A clarifying comment in
the runner (e.g., `# Does not include track doc — manually written`)
or a separate `card_authorized_write_paths` field in the summary would
make the distinction self-documenting. Cosmetic only; no scope,
security, or evidence concern.

No further missing items.

---

## [Sharper Question]

Does the delta evidence packet provide a sound baseline for a future
C3-A acceptance gate to use when considering whether the
`if_expr_internal_compiler_delta` label may be referenced in future
compiler capability documentation — without implying public demo or
release execution?

Answer: Yes, with the boundary it establishes. The packet provides:

1. A machine-readable `evidence_label` and `evidence_class` that a
   future capability documentation card can cite by label only, without
   citing the proof JSON as public evidence.
2. A `non_claims` block that explicitly prohibits
   `no_public_demo_claim`, `no_stable_production_all_grammar_claim`,
   and `no_spark_claim` — making it safe for C3-A to carry these
   prohibitions into any future capability documentation gate without
   re-deriving them.
3. A live SHA256 anchor chain that confirms the historical release
   evidence files have not been modified, establishing a clear boundary
   between pre-R190 historical exclusion evidence and post-R190
   compiler-only delta evidence.

A future documentation or capability-reporting card may reference the
delta packet's label and class without creating public claim authority,
provided the referring card carries its own non-claims block and cites
C1-A as the authorization basis for the delta label.

---

## [Route]

**Verdict: proceed — 11/11 PASS, no blockers.**

```text
checks total: 11
checks pass:  11
checks fail:  0
blockers:     none
non-blocking notes: 1

NB-1: closed_surface_scan.authorized_write_paths records runner write scope only,
      not full C1-A card write scope (which also includes track doc); architecturally
      correct — track doc cannot be written by runner; cosmetic doc note only;
      no scope, security, or evidence impact; not a blocker for C3-A acceptance
```

**Exact recommendation for C3-A:**

```text
Accept if_expr delta proof.

1. Accept evidence_label: if_expr_internal_compiler_delta and
   evidence_class: post_alpha_compiler_only_delta as correct.
   These are the only labels authorized by C1-A. Generated outputs
   must not be called official first-RC, alpha, release, public demo,
   runtime, production, Spark, or all-grammar evidence.

2. Accept D-1..D-13 proof result (39/39 sub-checks PASS).
   All required proof matrix items from C1-A are satisfied.

3. Accept old evidence immutability. SHA256 of historical harness
   summary independently confirmed: bc8d69f65c9267a604cb47e8ce0498a8373a80eaa264a2c53892139552a2618b.
   All three historical packets (compiler_release_acceptance_harness_summary.json,
   official_first_rc_evidence_summary.json, combined_post_prep_smoke_summary.json)
   remain unchanged.

4. Accept that the historical branch_conditional_if_expr excluded-feature
   marker is preserved without rewriting. The delta packet adds a new
   post-alpha compiler-only evidence layer; it does not modify the
   historical S3-R164-C4-A exclusion basis.

5. Carry NB-1 as a cosmetic structural note: future proof runners should
   consider documenting the distinction between runner-local write scope
   (DELTA_AUTHORIZED_WRITE_PATHS) and full card write scope (C1-A
   authorized paths). Non-blocking for acceptance.

6. Keep all closed surfaces closed:
   runtime/evaluator, release execution, release harness mutation,
   public claims, Spark, API/CLI widening, TypeChecker/SemanticIR
   behavior changes, docs/spec, production.

7. Status to record: done / delta-proof-accepted /
   if_expr_internal_compiler_delta.
```

Route: `track` — accept delta proof; record
`if_expr_internal_compiler_delta` evidence as accepted; keep all
closed surfaces closed pending separate authorization.
