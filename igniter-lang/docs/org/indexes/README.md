# Org Indexes

Status: initialized
Owner: [Org Architect Supervisor]

This directory is reserved for compact org-sidecar indexes.

Allowed index types:

```text
orientation indexes
role-instance memory indexes
documentation metabolism indexes
process-pattern indexes
```

Indexes here are navigation aids only. They do not replace cards, tracks,
gates, proposals, current-status, or role profiles.
