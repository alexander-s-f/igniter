# S3-R194 - if_expr Release Harness Delta Authorization Review

Status: planned
Date: 2026-05-27

Dispatch: R194 = C1-A -> C2-S

Pattern:
- C1-A decides whether a bounded compiler-only release-harness delta proof may
  open after R193 proof-summary hygiene closure.
- C2-S curates the decision and preserves the exact next Main Line boundary.

Round intent:
- Decide whether `if_expr` internal compiler support is ready to receive a
  separate post-alpha delta evidence packet.
- Preserve historical alpha/first-RC/release evidence as immutable.
- Prevent accidental release, public, runtime, Spark, or API/CLI claims.

Current accepted state:

```text
R190: if_expr v0 implementation accepted as internal compiler support
R191: docs/spec sync accepted
R192: accepted release evidence remains historical / unchanged / immutable
R193: proof-summary hygiene accepted, 28/28 PASS preserved
release lane: paused
runtime/evaluator: closed
public claims: closed
Spark/API/CLI: closed
```

Global constraints:
- Do not run a release-harness delta proof in this round.
- Do not mutate accepted alpha / first-RC / release evidence.
- Do not mutate existing release harness corpus unless explicitly authorized in
  a later proof card.
- Do not execute release commands.
- Do not publish/yank gems.
- Do not create/delete/push tags.
- Do not sign or deploy.
- Do not authorize public demo, stable, production, all-grammar, Spark,
  runtime/evaluator, profile discovery/defaulting/finalization, or public
  API/CLI widening claims.
- Do not authorize parser, TypeChecker, SemanticIR, compiler behavior,
  assembler, loader/report, CompatibilityReport, `.igapp`, manifest, golden,
  artifact-hash, runtime, Ledger/TBackend, BiHistory, stream/OLAP, cache, or
  production changes.

---

## Card S3-R194-C1-A

```text
Card: S3-R194-C1-A
Agent: [Portfolio Architect Supervisor]
Role: portfolio-architect-supervisor
Track: branch-conditional-if-expr-release-harness-delta-authorization-review-v0

Route: UPDATE
Depends on:
- S3-R193-C4-S

Goal:
Decide whether a bounded compiler-only if_expr release-harness delta proof may
begin now that proof-summary hygiene is accepted.

Scope:
- Read:
  - igniter-lang/docs/tracks/stage3-round193-status-curation-v0.md
  - igniter-lang/docs/tracks/branch-conditional-if-expr-proof-summary-hygiene-acceptance-decision-v0.md
  - igniter-lang/docs/tracks/branch-conditional-if-expr-proof-summary-hygiene-v0.md
  - igniter-lang/docs/discussions/branch-conditional-if-expr-proof-summary-hygiene-pressure-v0.md
  - igniter-lang/experiments/branch_conditional_if_expr_v0_implementation_proof/out/branch_conditional_if_expr_v0_implementation_proof_summary.json
  - igniter-lang/docs/tracks/branch-conditional-if-expr-post-implementation-release-harness-delta-decision-v0.md
  - igniter-lang/docs/tracks/stage3-round192-status-curation-v0.md
  - igniter-lang/docs/tracks/branch-conditional-if-expr-docs-spec-sync-v0.md
  - igniter-lang/docs/tracks/branch-conditional-if-expr-v0-implementation-acceptance-decision-v0.md
  - igniter-lang/experiments/compiler_release_acceptance_harness_v0/out/compiler_release_acceptance_harness_summary.json
  - igniter-lang/experiments/compiler_release_official_first_rc_evidence_v0/out/official_first_rc_evidence_summary.json
  - igniter-lang/experiments/compiler_release_combined_post_prep_smoke_v0/out/**/combined_post_prep_smoke_summary.json if needed
- Decide:
  - authorize a bounded compiler-only release-harness delta proof next;
  - authorize only more design/prep;
  - hold;
  - redirect to runtime/evaluator design-only route;
  - redirect to another compiler/language route;
  - pause.
- If authorizing a later proof card, define exact:
  - proof label, recommended:
    - `if_expr_internal_compiler_delta`
  - write/output scope;
  - new experiment directory;
  - summary/result packet shape;
  - old-evidence immutability checks;
  - compiler-only proof matrix;
  - release-scope/excluded-feature assertions;
  - public non-claims;
  - command matrix;
  - closed surfaces.
- Explicitly answer:
  - whether a release-harness delta proof may open next;
  - whether this card itself runs or authorizes execution of that proof;
  - whether generated outputs may be called official first-RC, alpha, release,
    or public demo evidence;
  - whether accepted alpha / first-RC / release evidence remains unchanged;
  - whether the historical `branch_conditional_if_expr` excluded-feature marker
    remains historically valid;
  - whether the delta, if opened, must be a new evidence packet rather than a
    rewrite of old evidence;
  - whether runtime/evaluator support remains closed;
  - whether public release/demo/stable/production/all-grammar claims remain
    closed;
  - whether Spark/API/CLI remain closed;
  - whether TypeChecker/SemanticIR/compiler behavior changes remain closed.
- Do not run proof commands in this card.
- Do not execute release commands.
- Do not publish/yank gems.
- Do not create/delete/push tags.
- Do not authorize public claims.

Default candidate next proof boundary, if authorized:
- Card: `S3-R195-C1-I`
- Track: `branch-conditional-if-expr-release-harness-delta-proof-v0`
- Route: UPDATE
- Allowed write scope:
  - `igniter-lang/experiments/branch_conditional_if_expr_release_harness_delta_v0/**`
  - `igniter-lang/docs/tracks/branch-conditional-if-expr-release-harness-delta-proof-v0.md`
- Candidate behavior:
  - build a new harness-local evidence packet for `if_expr` internal compiler
    support;
  - use evidence label `if_expr_internal_compiler_delta`;
  - prove TypeChecker + typed SemanticIR support only;
  - assert no runtime/evaluator/lazy branch execution support;
  - assert no public API/CLI/Spark/release/demo/stable/all-grammar claim;
  - assert accepted alpha/first-RC/release evidence files are immutable;
  - preserve historical evidence as historical, not rewritten.
- Candidate proof matrix:
  - positive minimal `if_expr` compile/typed-SemanticIR;
  - positive nested `if_expr` compile/typed-SemanticIR;
  - negative non-Bool condition `OOF-IF1`;
  - negative missing else `OOF-IF2`;
  - negative branch mismatch `OOF-IF3`;
  - negative empty/non-value branch `OOF-IF4`;
  - `OOF-IF5` absent / non-status;
  - unsupported-`if_expr` `OOF-TY0` absent;
  - derivative `OOF-TY0` secondary-labeled where present;
  - release evidence immutability checks;
  - closed-surface scan.

Deliver:
- Authorization decision doc in `igniter-lang/docs/tracks/` or
  `igniter-lang/docs/gates/`
- Compact decision summary
- If authorized: exact next proof card boundary
- If held/redirected: exact blocker list
```

---

## Card S3-R194-C2-S

```text
Card: S3-R194-C2-S
Agent: [Status Curator]
Role: status-curator
Track: stage3-round194-status-curation-v0

Route: UPDATE
Depends on:
- S3-R194-C1-A

Goal:
Curate the R194 release-harness delta authorization decision and preserve the
exact next Main Line boundary.

Scope:
- Read:
  - S3-R194-C1-A decision
  - igniter-lang/docs/tracks/stage3-round193-status-curation-v0.md
  - igniter-lang/docs/current-status.md
  - igniter-lang/docs/cards/S3/S3-R194.md
- Record:
  - whether a delta proof is authorized, held, paused, or redirected;
  - exact next card boundary or blocker list;
  - accepted release evidence historical/unchanged status;
  - runtime/evaluator status;
  - public claim status;
  - Spark/API/CLI status;
  - TypeChecker/SemanticIR/compiler behavior status;
  - remaining closed surfaces.
- If C1-A authorizes a proof route, preserve it as a future proof card only;
  do not run it or widen the scope.
- Do not authorize release execution, public claims, implementation,
  runtime/evaluator, Spark, API/CLI, or compiler behavior changes.

Deliver:
- Status curation doc in `igniter-lang/docs/tracks/`
- Compact round outcome table
- Current-status delta if needed
- Exact next dispatch recommendation
```
