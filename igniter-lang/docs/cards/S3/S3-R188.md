# S3-R188 - Branch Conditional if_expr Semantics Proof

Status: planned
Date: 2026-05-27

Dispatch: R188 = C1-P1 -> C2-X -> C3-A -> C4-S

Pattern:
- C1-P1 runs the proof-only semantics fixture.
- C2-X pressure-runs after C1 output lands.
- C3-A decides after C1/C2.
- C4-S curates after C3-A.

Round intent:
- Prove the accepted `if_expr` v0 semantic shape before any implementation
  authorization review.
- Keep the current compiler behavior visible: parser accepts `if_expr`, but
  TypeChecker refuses it with `OOF-TY0` until implementation is separately
  authorized.
- Resolve the three binding R187 proof gates:
  - drop or resolve `OOF-IF5`;
  - pin canonical `Bool` representation;
  - choose SemanticIR branch shape.

Global constraints:
- Proof-only route.
- Do not implement compiler support.
- Do not authorize implementation.
- Do not edit parser, TypeChecker, SemanticIR, assembler, runtime, artifacts,
  goldens, public API/CLI, release evidence, or Spark code.
- Do not execute release commands.
- Do not publish/yank gems.
- Do not create/delete/push tags.
- Do not sign or deploy.
- Do not authorize public demo, production, stable, all-grammar, Spark,
  runtime, profile discovery/defaulting/finalization, analyzer/tracer/
  visualizer implementation, or public API/CLI widening claims.

Current accepted state:

```text
R187 decision: accepted if_expr v0 design as proof boundary
current source parser: emits kind: if_expr
current TypeChecker behavior: OOF-TY0 Unsupported expression kind: if_expr
implementation authorization: closed
release lane: paused
```

---

## Card S3-R188-C1-P1

```text
Card: S3-R188-C1-P1
Agent: [Compiler/Grammar Expert]
Role: compiler-grammar-expert
Track: branch-conditional-if-expr-semantics-proof-v0

Route: UPDATE
Depends on:
- S3-R187-C4-A

Goal:
Build a proof-only semantics fixture for branch/conditional `if_expr` v0,
without compiler implementation, to validate the accepted design before any
implementation-authorization review.

Scope:
- Read:
  - igniter-lang/docs/tracks/branch-conditional-if-expr-next-route-decision-v0.md
  - igniter-lang/docs/tracks/branch-conditional-if-expr-scope-and-semantics-design-v0.md
  - igniter-lang/docs/tracks/branch-conditional-if-expr-current-surface-and-evidence-survey-v0.md
  - igniter-lang/docs/discussions/branch-conditional-if-expr-design-pressure-v0.md
  - igniter-lang/docs/tracks/stage3-round187-status-curation-v0.md
  - igniter-lang/lib/igniter_lang/parser.rb
  - igniter-lang/lib/igniter_lang/typechecker.rb
  - igniter-lang/lib/igniter_lang/semanticir_emitter.rb
  - release harness summary/evidence files cited by R187
- Create proof-local artifacts under:
  - igniter-lang/experiments/branch_conditional_if_expr_semantics_proof_v0/**
  - igniter-lang/docs/tracks/branch-conditional-if-expr-semantics-proof-v0.md
- Prove:
  - parser accepts minimal if/else and produces current `if_expr` AST shape;
  - current mainline compiler still refuses `if_expr` with canonical `OOF-TY0`;
  - proof-local model accepts Bool condition and same branch type;
  - proof-local model rejects non-Bool condition as future `OOF-IF1`;
  - proof-local model rejects missing else as future `OOF-IF2`;
  - proof-local model rejects branch type mismatch as future `OOF-IF3`;
  - proof-local model rejects empty/non-value branch as future `OOF-IF4`;
  - `OOF-IF5` is dropped from proof scope unless one owner and one trigger are
    explicitly chosen before modeling;
  - canonical Bool representation is pinned from live TypeChecker evidence;
  - SemanticIR branch shape is chosen and justified;
  - union dependencies are modeled;
  - nested `if_expr` under the same rules is modeled;
  - release harness remains out_of_scope before implementation;
  - closed-surface scan shows no parser/TypeChecker/SemanticIR/assembler,
    artifact/golden, release, runtime, public API/CLI, or Spark edits.
- Optional:
  - clean stale harness README HOLD note only if explicitly recorded as
    docs-hygiene in the proof track.

Expected command/proof matrix:
- targeted parser probe;
- targeted TypeChecker refusal probe;
- proof-local semantics model run;
- proof-local SemanticIR-shape model run;
- closed-surface scan;
- summary JSON generation.

Do not:
- implement compiler support;
- edit parser, TypeChecker, SemanticIR, assembler, artifacts, goldens, runtime,
  release evidence, public docs, public API/CLI, or Spark code unless the
  optional README hygiene write is explicitly selected and remains docs-only;
- authorize implementation;
- reopen release work.

Deliver:
- Proof track in `igniter-lang/docs/tracks/`
- Proof-local experiment summary JSON
- Compact proof matrix
- Closed-surface scan
- Recommendation: implementation-authorization review, more proof, or hold
```

---

## Card S3-R188-C2-X

```text
Card: S3-R188-C2-X
Agent: [External Pressure Reviewer]
Role: external-pressure-reviewer
Track: branch-conditional-if-expr-semantics-proof-pressure-v0

Route: UPDATE
Depends on:
- S3-R188-C1-P1

Goal:
Pressure-review the proof-only `if_expr` semantics fixture and decide whether
it is sufficient to consider a later implementation-authorization review.

Scope:
- Read:
  - S3-R188-C1-P1 output
  - igniter-lang/docs/tracks/branch-conditional-if-expr-semantics-proof-v0.md
  - igniter-lang/experiments/branch_conditional_if_expr_semantics_proof_v0/out/**/branch_conditional_if_expr_semantics_proof_summary.json
  - igniter-lang/docs/tracks/branch-conditional-if-expr-next-route-decision-v0.md
  - igniter-lang/docs/tracks/stage3-round187-status-curation-v0.md
- Verify:
  - all required proof cases are present and PASS or explicitly held;
  - current `OOF-TY0` refusal remains preserved before implementation;
  - `OOF-IF1..OOF-IF4` proof-local diagnostics match accepted v0 semantics;
  - `OOF-IF5` is dropped or has one owner/trigger;
  - canonical Bool representation is pinned from live evidence;
  - SemanticIR branch shape is chosen and justified;
  - union dependency behavior is modeled;
  - nested `if_expr` remains under same rules;
  - release harness remains out_of_scope before implementation;
  - no compiler/runtime/release/Spark/public API edits or claims are introduced.
- Decide:
  - proceed;
  - proceed with notes;
  - hold;
  - redirect.

Do not:
- edit files;
- implement code;
- authorize implementation;
- run release commands.

Deliver:
- Discussion doc in `igniter-lang/docs/discussions/`
- Compact pressure verdict
- Exact blockers or proceed recommendation
- Recommended C3-A decision boundary
```

---

## Card S3-R188-C3-A

```text
Card: S3-R188-C3-A
Agent: [Portfolio Architect Supervisor]
Role: portfolio-architect-supervisor
Track: branch-conditional-if-expr-semantics-proof-acceptance-decision-v0

Route: UPDATE
Depends on:
- S3-R188-C1-P1
- S3-R188-C2-X

Goal:
Accept, conditionally accept, hold, or redirect the proof-only `if_expr`
semantics fixture and decide whether an implementation-authorization review may
open later.

Scope:
- Read:
  - C1-P1 proof output
  - C2-X pressure verdict
  - igniter-lang/docs/tracks/branch-conditional-if-expr-semantics-proof-v0.md
  - igniter-lang/experiments/branch_conditional_if_expr_semantics_proof_v0/out/**/branch_conditional_if_expr_semantics_proof_summary.json
  - igniter-lang/docs/tracks/branch-conditional-if-expr-next-route-decision-v0.md
  - igniter-lang/docs/tracks/stage3-round187-status-curation-v0.md
- Decide:
  - accept proof;
  - conditional accept with exact follow-up;
  - hold pending proof fixes;
  - redirect to more design;
  - authorize a later implementation-authorization review;
  - keep implementation held.
- Explicitly answer:
  - whether the proof-only semantics fixture is accepted;
  - whether `OOF-IF1..OOF-IF4` proof vocabulary is stable enough for
    implementation authorization review;
  - whether `OOF-IF5` is dropped/resolved;
  - whether canonical Bool representation is accepted;
  - whether SemanticIR branch shape is accepted;
  - whether current `OOF-TY0` refusal remains accepted until implementation;
  - whether implementation authorization review may open next or must wait;
  - whether release lane remains paused;
  - whether public demo/production/all-grammar/Spark/runtime/API widening
    claims remain closed.

Do not:
- implement code;
- execute release commands;
- authorize public demo, stable, production, all-grammar, Spark, runtime,
  profile discovery/defaulting, or API/CLI widening claims.

Deliver:
- Portfolio decision doc in `igniter-lang/docs/tracks/` or
  `igniter-lang/docs/gates/`
- Compact decision summary
- Exact next dispatch recommendation or blocker list
```

---

## Card S3-R188-C4-S

```text
Card: S3-R188-C4-S
Agent: [Status Curator]
Role: status-curator
Track: stage3-round188-status-curation-v0

Route: UPDATE
Depends on:
- S3-R188-C3-A

Goal:
Curate R188 proof outcome into Stage 3 status and preserve the exact next-route
boundary for `if_expr`.

Scope:
- Read:
  - S3-R188-C1-P1 proof output
  - S3-R188-C2-X pressure verdict
  - S3-R188-C3-A decision
  - igniter-lang/docs/current-status.md
  - igniter-lang/docs/cards/S3/S3-R188.md
- Update only if needed:
  - `igniter-lang/docs/current-status.md`
  - established status/index pointers used by recent rounds.
- Record:
  - R188 outcome table;
  - proof acceptance/hold status;
  - exact next route;
  - current `OOF-TY0` status;
  - release lane still paused;
  - closed surfaces.

Do not:
- alter design decisions;
- authorize implementation;
- run release commands;
- edit compiler/runtime code.

Deliver:
- Status curation track in `igniter-lang/docs/tracks/`
- Compact handoff for the next round
```

---

## Expected R188 Outcome

Preferred outcome:

```text
if_expr proof-only semantics fixture accepted
implementation remains closed unless C3-A explicitly opens a later
implementation-authorization review
current OOF-TY0 refusal remains accepted until implementation lands
release lane remains paused
```

If blockers appear:

```text
hold implementation authorization
route proof fixes or additional design
preserve current OOF-TY0 unsupported boundary
```
