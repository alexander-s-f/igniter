# S3-R44

Status: closed
Date: 2026-05-14
Closed: 2026-05-14
Lineup: PROP-036 post-orchestrator negative scan -> CLI/API exposure decision -> implementation/pressure/status if authorized

Dispatch:
R44 = [C1-P1] -> C2-A -> [C3-I] -> [C4-P2, C5-X] -> C6-S

C1 runs first as a safety scan before any public caller surface.
C2 is an Architect decision.
C3 runs only if C2 authorizes it.
C4+C5 run after C3 if implementation lands.
C6 closes/maps the round.

---

```text
Card: S3-R44-C1-P1
Agent: [Igniter-Lang Research Agent]
Role: research-agent
Track: prop036-post-orchestrator-negative-artifact-scan-v0

Route: UPDATE

Goal:
Prove that the current PROP-036 assembler/orchestrator implementation does not
leak loader-status vocabulary or runtime-readiness vocabulary into any written
JSON artifact or refusal report before public CLI/API exposure is considered.

Context:
- R43 C3 pressure found no blocker, but recommended broadening the negative scan
  beyond successful manifests.
- Current implementation state:
  - source finalization proof PASS 22/22;
  - assembler field proof PASS 19/19;
  - orchestrator pass-through proof PASS 11/11;
  - post-orchestrator regression PASS.

Scope:
- Read:
  - igniter-lang/docs/tracks/prop036-orchestrator-profile-source-pass-through-v0.md
  - igniter-lang/docs/discussions/r43-orchestrator-profile-source-pressure-v0.md
  - igniter-lang/docs/tracks/prop036-post-orchestrator-regression-chain-v0.md
  - igniter-lang/docs/tracks/assembler-compiler-profile-id-field-v0.md
- Run or reuse current PROP-036 proof outputs.
- Scan all JSON written by the relevant proof directories for forbidden vocabulary:
  - loader status vocabulary: `absent_legacy`, `present_verified`, `mismatch`,
    `missing_required`
  - runtime readiness vocabulary: `runtime_ready`, `evaluation_ready`,
    `gate3_authorized`, `runtime_authority`, `production_ready`
- Include refusal reports, summary JSON, manifest JSON, and compilation reports.
- Do not implement code.
- Do not mutate goldens.

Deliver:
- Track doc in `igniter-lang/docs/tracks/`
- Exact command matrix and PASS/FAIL
- List of scanned paths
- Any allowed terms with rationale, if false positives appear
- Recommendation: ready / not ready for CLI/API exposure decision
```

```text
Card: S3-R44-C2-A
Agent: [Architect Supervisor / Codex]
Role: architect-supervisor
Track: prop036-cli-api-exposure-authorization-review-v0

Route: UPDATE
Depends on:
- S3-R44-C1-P1

Goal:
Decide whether a bounded public caller surface may expose caller-supplied
`compiler_profile_source` after the post-orchestrator negative scan.

Scope:
- Read:
  - S3-R44-C1-P1 output
  - igniter-lang/docs/gates/prop036-orchestrator-wiring-authorization-review-v0.md
  - igniter-lang/docs/tracks/prop036-orchestrator-profile-source-pass-through-v0.md
  - igniter-lang/docs/proposals/PROP-036-compiler-profile-manifest-identity-v0.md
  - relevant CLI/API entry files discovered by `rg "CompilerOrchestrator|compile\\(" igniter-lang`
- Decide: authorize / hold / redirect / reject.
- If authorize, define exact C3-I boundary:
  - public surface(s) allowed;
  - source input shape;
  - how callers supply the finalized object;
  - refusal behavior;
  - proof matrix.
- Preserve `legacy_optional`.
- Do not authorize profile finalization/discovery/defaulting in CLI/API.
- Do not authorize golden migration.
- Do not authorize loader/report, CompatibilityReport, `.ilk`, receipts,
  signing, dispatch migration, RuntimeMachine, Ledger/TBackend, BiHistory,
  stream/OLAP, cache, or production behavior.

Deliver:
- Decision doc in `igniter-lang/docs/gates/`
- Compact decision summary
- Exact C3-I implementation card boundary or blocker list
```

```text
Card: S3-R44-C3-I
Agent: [Igniter-Lang Implementation Agent]
Role: implementation-agent
Track: prop036-ruby-facade-profile-source-exposure-v0

Route: UPDATE
Depends on:
- S3-R44-C2-A approved-bounded-ruby-facade-exposure

Stage Refresh:
- Re-read role onboarding/profile if this is a fresh session.

Goal:
Expose caller-supplied finalized `compiler_profile_source` through the public
Ruby facade `IgniterLang.compile`, preserving nil legacy behavior.

Authorized files:
- igniter-lang/lib/igniter_lang.rb
- igniter-lang/experiments/prop036_ruby_facade_profile_source_exposure/
- igniter-lang/docs/tracks/prop036-ruby-facade-profile-source-exposure-v0.md

Allowed implementation:
- Add optional keyword `compiler_profile_source: nil` to `IgniterLang.compile`.
- Forward the keyword unchanged to `CompilerOrchestrator#compile`.
- Preserve all existing keywords and default behavior.
- Preserve `legacy_optional` when the keyword is nil.

Source input shape:
- Caller supplies the already-finalized source object/hash accepted by the
  existing assembler/orchestrator validation path.
- `IgniterLang.compile` must not finalize, discover, infer, load, normalize from
  a path, or default a compiler profile source.

Refusal behavior:
- Invalid non-nil source must refuse through the existing assembler/orchestrator
  compiler-profile-source refusal path.
- No loader-status vocabulary or runtime-readiness vocabulary may appear as
  exact JSON keys or scalar values in newly written JSON or refusal artifacts.

Proof matrix:
- `IgniterLang.compile(..., compiler_profile_source: nil)` succeeds and produces
  legacy output without `compiler_profile_id`.
- `IgniterLang.compile(..., compiler_profile_source: valid_source)` succeeds and
  writes top-level `compiler_profile_id`.
- `IgniterLang.compile(..., compiler_profile_source: invalid_source)` refuses
  before profiled artifact output.
- Existing CLI behavior remains unchanged.
- Negative vocabulary scan over newly written JSON/refusal artifacts reports
  exact forbidden-token hits: 0.

Constraints:
- Do not edit `igniter-lang/lib/igniter_lang/cli.rb`, `bin/igc`, or
  `bin/igniter-lang`.
- Do not implement CLI flags, path-based source loading, inline JSON parsing,
  profile finalization, discovery, defaulting, loader/report,
  CompatibilityReport, golden migration, RuntimeMachine, dispatch migration,
  Ledger/TBackend, cache, or production behavior.

Deliver:
- Updated authorized files only
- Proof script + summary JSON if required by C2-A
- Track doc in `igniter-lang/docs/tracks/`
- Exact PASS/FAIL command matrix
- Remaining blockers
```

```text
Card: S3-R44-C4-P2
Agent: [Igniter-Lang Research Agent]
Role: research-agent
Track: prop036-post-cli-api-exposure-regression-chain-v0

Route: UPDATE
Depends on:
- S3-R44-C3-I delivered

Goal:
Run targeted regression after any C3 implementation lands.

Scope:
- Read C3 handoff/track.
- Run C3 proof.
- Re-run C1 negative artifact scan if the public surface writes JSON/refusal reports.
- Re-run relevant existing compiler CLI/API smoke.
- Confirm nil/default behavior remains legacy optional.
- Do not implement code.

Deliver:
- Track doc in `igniter-lang/docs/tracks/`
- Exact command matrix and PASS/FAIL
- Recommendation: ready / not ready for next Architect decision
```

```text
Card: S3-R44-C5-X
Agent: [Igniter-Lang External Pressure Reviewer]
Role: external-pressure-reviewer
Borrowed lens: runtime-pressure
Track: prop036-cli-api-profile-source-pressure-v0

Route: UPDATE
Depends on:
- S3-R44-C3-I delivered

Goal:
Pressure-review any new public CLI/API `compiler_profile_source` surface for
authority widening and vocabulary leakage.

Scope:
- Read:
  - S3-R44-C2-A decision
  - S3-R44-C3-I track
  - S3-R44-C1-P1 negative scan
- Check:
  - no profile finalization/discovery/defaulting in public surface;
  - invalid source refuses before profiled artifact output;
  - nil/default source stays legacy optional;
  - no loader-status vocabulary leaks into assembler/orchestrator refusal;
  - no runtime/dispatch/production readiness implication.
- Do not implement code.
- Pressure only; do not create canon.

Deliver:
- Discussion doc in `igniter-lang/docs/discussions/`
- Verdict: proceed / proceed-with-notes / hold
- Blockers vs non-blockers
```

```text
Card: S3-R44-C6-S
Agent: [Igniter-Lang Status Curator]
Role: status-curator
Track: stage3-round44-status-curation-v0

Route: UPDATE
Depends on:
- S3-R44-C1-P1
- S3-R44-C2-A
- S3-R44-C3-I if authorized
- S3-R44-C4-P2 if run
- S3-R44-C5-X if run

Goal:
Close/map R44 and update the new cards dispatch layer.

Scope:
- Mark this round file closed and append Round Receipt.
- Update `igniter-lang/docs/cards/S3/S3.md` round index.
- Update `igniter-lang/docs/current-status.md`.
- Update `igniter-lang/docs/tracks/README.md`.
- Update `igniter-lang/docs/gates/README.md` if C2-A creates a gate decision.
- Do not create new semantics.
- Do not authorize implementation.

Deliver:
- Updated R44 round receipt
- Updated status/map docs
- Track doc in `igniter-lang/docs/tracks/`
- Compact R44 summary + R45 recommendation
```

---

## Round Receipt

Status: closed by S3-R44-C6-S.

Landed evidence:
- C1-P1 `prop036-post-orchestrator-negative-artifact-scan-v0.md`: PASS; refreshed source finalization, assembler field, and orchestrator pass-through proofs; scanned 49 JSON files with 0 exact forbidden loader/runtime readiness token hits.
- C2-A `prop036-cli-api-exposure-authorization-review-v0.md`: approved bounded Ruby facade exposure only. `IgniterLang.compile(..., compiler_profile_source:)` may forward a caller-supplied finalized source object; CLI flags, path/inline JSON loading, discovery, defaulting, loader/report, CompatibilityReport, golden migration, dispatch, runtime, and production behavior remain closed.
- C3-I `prop036-ruby-facade-profile-source-exposure-v0.md`: landed optional `compiler_profile_source: nil` on `IgniterLang.compile`, forwarding unchanged to `CompilerOrchestrator#compile`; proof PASS 7/7; exact forbidden-token scan over 29 JSON/refusal artifacts had 0 hits.
- C4-P2 `prop036-post-cli-api-exposure-regression-chain-v0.md`: targeted regression PASS; source/facade/orchestrator/assembler/production CLI paths keep legacy nil behavior; combined exact forbidden-token scan covered 88 JSON files with 0 hits.
- C5-X `prop036-cli-api-profile-source-pressure-v0.md`: verdict `proceed-with-notes`; no blockers. Non-blockers: caller-facing source-shape docs, explicit transport-only facade contract, and a tracking item before CLI exposure.
- C6-S `stage3-round44-status-curation-v0.md`: maps updated; no new semantics or implementation authorization added.

R45 recommendation:
- Choose one bounded PROP-036 follow-up. Preferred next slice is CLI exposure design/tracking only: decide path vs inline JSON vs facade-object input, refusal wording, nil/no-flag legacy proof, negative scan coverage, and pressure review before any CLI implementation. Alternative bounded slices remain loader/report status, CompatibilityReport compiler-profile section, or exact golden migration list/hash-churn planning.
