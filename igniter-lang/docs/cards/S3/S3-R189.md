# S3-R189 - Branch Conditional if_expr Implementation Authorization

Status: planned
Date: 2026-05-27

Dispatch: R189 = C1-A -> C2-I (if authorized) -> C3-S

Pattern:
- C1-A performs implementation-authorization review.
- C2-I runs only if C1-A explicitly authorizes bounded implementation.
- C3-S curates the round after C1-A and, if run, C2-I.

Round intent:
- Decide whether the accepted `if_expr` v0 design and proof are ready for a
  bounded first implementation.
- If authorized, implement only the exact internal compiler slice named by C1-A.
- Preserve release lane pause and all public/demo/runtime/Spark claims closed.

Global constraints:
- Do not implement code in C1-A.
- C2-I may run only if C1-A authorizes it explicitly.
- Do not execute release commands.
- Do not publish/yank gems.
- Do not create/delete/push tags.
- Do not sign or deploy.
- Do not authorize public demo, production, stable, all-grammar, Spark,
  runtime, profile discovery/defaulting/finalization, analyzer/tracer/
  visualizer implementation, or public API/CLI widening claims.
- Runtime evaluator support remains out of scope unless C1-A explicitly names a
  proof-only or implementation boundary for it; default is closed.

Current accepted state:

```text
R187: if_expr v0 design accepted
R188: proof-only semantics fixture accepted, 14/14 PASS
current source parser: emits kind: if_expr
current TypeChecker behavior: OOF-TY0 Unsupported expression kind: if_expr
next route: implementation-authorization review only
release lane: paused
```

---

## Card S3-R189-C1-A

```text
Card: S3-R189-C1-A
Agent: [Portfolio Architect Supervisor]
Role: portfolio-architect-supervisor
Track: branch-conditional-if-expr-implementation-authorization-review-v0

Route: UPDATE
Depends on:
- S3-R188-C3-A

Goal:
Decide whether a bounded first implementation of `if_expr` v0 may begin, using
the accepted R187 design and R188 proof-only semantics fixture.

Scope:
- Read:
  - igniter-lang/docs/tracks/branch-conditional-if-expr-semantics-proof-acceptance-decision-v0.md
  - igniter-lang/docs/tracks/branch-conditional-if-expr-semantics-proof-v0.md
  - igniter-lang/docs/discussions/branch-conditional-if-expr-semantics-proof-pressure-v0.md
  - igniter-lang/experiments/branch_conditional_if_expr_semantics_proof_v0/out/branch_conditional_if_expr_semantics_proof_summary.json
  - igniter-lang/docs/tracks/branch-conditional-if-expr-next-route-decision-v0.md
  - igniter-lang/docs/tracks/branch-conditional-if-expr-current-surface-and-evidence-survey-v0.md
  - igniter-lang/docs/tracks/stage3-round188-status-curation-v0.md
  - igniter-lang/lib/igniter_lang/parser.rb
  - igniter-lang/lib/igniter_lang/typechecker.rb
  - igniter-lang/lib/igniter_lang/semanticir_emitter.rb
  - igniter-lang/lib/igniter_lang/compiler_orchestrator.rb
- Decide:
  - authorize bounded implementation;
  - authorize only implementation prep;
  - hold pending more proof/design;
  - redirect.
- If authorizing implementation, define exact:
  - write scope;
  - TypeChecker output shape;
  - SemanticIR lowered shape and recursive lowering rule;
  - diagnostic vocabulary (`OOF-IF1..OOF-IF4`, no `OOF-IF5`);
  - proof/regression matrix;
  - artifact/golden policy;
  - release harness policy;
  - runtime/evaluator stance;
  - closed surfaces.
- Must explicitly answer:
  - whether recursive SemanticIR lowering consistency is required;
  - whether TypeChecker and SemanticIR stage key conventions are separated;
  - whether empty-branch dependency policy is accepted;
  - whether current `OOF-TY0` remains until implementation lands;
  - whether runtime support is in or out of scope;
  - whether release/public demo/all-grammar claims remain closed;
  - whether C2-I may run in this round.

Default candidate implementation boundary, if authorized:
- write scope:
  - `igniter-lang/lib/igniter_lang/typechecker.rb`
  - `igniter-lang/lib/igniter_lang/semanticir_emitter.rb`
  - `igniter-lang/experiments/branch_conditional_if_expr_v0_implementation_proof/**`
  - `igniter-lang/docs/tracks/branch-conditional-if-expr-v0-implementation-v0.md`
- behavior:
  - support expression-level `if_expr` only;
  - require `else`;
  - condition must resolve to canonical Bool `{ "name": "Bool", "params": [] }`;
  - then/else branch final values must exact-match types;
  - branch must be value-producing;
  - nested `if_expr` follows same rules;
  - union dependencies only;
  - TypeChecker typed shape may keep `cond` / `then` / `else` with branch wrappers;
  - SemanticIR lowered shape must use `condition` / `then_branch` /
    `else_branch` flat recursively;
  - no `OOF-IF5`;
  - current `OOF-TY0` replaced only for supported/diagnosed `if_expr` paths.
- proof/regression:
  - positive minimal if/else compile;
  - positive nested if/else compile;
  - negative non-Bool condition `OOF-IF1`;
  - negative missing else `OOF-IF2`;
  - negative branch mismatch `OOF-IF3`;
  - negative empty/non-value branch `OOF-IF4`;
  - current excluded release harness historical evidence remains untouched;
  - closed-surface scan.

Do not:
- implement code in this card;
- execute release commands;
- authorize public demo, stable, production, all-grammar, Spark, runtime,
  profile discovery/defaulting, or API/CLI widening claims.

Deliver:
- Authorization decision doc in `igniter-lang/docs/tracks/` or
  `igniter-lang/docs/gates/`
- Compact decision summary
- If authorize: exact C2-I implementation card boundary
- If hold: exact blocker list
```

---

## Card S3-R189-C2-I

```text
Card: S3-R189-C2-I
Agent: [Implementation Agent]
Role: implementation-agent
Track: branch-conditional-if-expr-v0-implementation-v0

Route: UPDATE
Depends on:
- S3-R189-C1-A

Goal:
Implement the bounded `if_expr` v0 slice exactly if, and only if, S3-R189-C1-A
authorizes implementation.

Scope:
- Read:
  - S3-R189-C1-A authorization decision
  - igniter-lang/docs/tracks/branch-conditional-if-expr-semantics-proof-acceptance-decision-v0.md
  - igniter-lang/docs/tracks/branch-conditional-if-expr-semantics-proof-v0.md
  - igniter-lang/experiments/branch_conditional_if_expr_semantics_proof_v0/out/branch_conditional_if_expr_semantics_proof_summary.json
  - exact source files named in the C1-A write scope
- Implement only the C1-A authorized write scope.
- Required behavior unless C1-A narrows differently:
  - expression-level `if_expr` only;
  - `else` required;
  - condition type canonical Bool;
  - branch type exact match;
  - value-producing branches;
  - nested `if_expr` same rules;
  - union dependencies;
  - TypeChecker and SemanticIR stage shapes separated;
  - SemanticIR recursive lowering consistency;
  - diagnostics `OOF-IF1..OOF-IF4`;
  - no `OOF-IF5`;
  - runtime evaluator support out of scope unless explicitly authorized.
- Produce proof artifacts under the path named by C1-A.

Do not:
- run if C1-A holds or does not explicitly authorize C2-I;
- widen public API/CLI;
- mutate release evidence or accepted alpha evidence;
- create/update release harness official evidence unless explicitly authorized;
- edit runtime evaluator, Spark, release, package, profile discovery/defaulting,
  loader/report, CompatibilityReport, or public docs outside C1-A scope;
- execute release commands.

Deliver:
- Implementation track doc in `igniter-lang/docs/tracks/`
- Changed file list
- Command matrix
- Proof/regression summary
- Closed-surface scan
- Exact remaining blockers or acceptance recommendation
```

---

## Card S3-R189-C3-S

```text
Card: S3-R189-C3-S
Agent: [Status Curator]
Role: status-curator
Track: stage3-round189-status-curation-v0

Route: UPDATE
Depends on:
- S3-R189-C1-A
- S3-R189-C2-I if run

Goal:
Curate R189 authorization/implementation outcome into Stage 3 status and
preserve the exact next-route boundary for `if_expr`.

Scope:
- Read:
  - S3-R189-C1-A decision
  - S3-R189-C2-I output if run
  - igniter-lang/docs/current-status.md
  - igniter-lang/docs/cards/S3/S3-R189.md
- Update only if needed:
  - `igniter-lang/docs/current-status.md`
  - established status/index pointers used by recent rounds.
- Record:
  - R189 outcome table;
  - whether implementation was authorized;
  - if C2-I ran, whether implementation landed or requires acceptance review;
  - exact next route;
  - release lane still paused;
  - closed surfaces.

Do not:
- alter authorization decisions;
- authorize implementation beyond C1-A;
- run release commands;
- edit compiler/runtime code.

Deliver:
- Status curation track in `igniter-lang/docs/tracks/`
- Compact handoff for the next round
```

---

## Expected R189 Outcome

Preferred outcome:

```text
C1-A authorizes bounded internal implementation
C2-I lands first if_expr v0 compiler slice or reports exact blockers
C3-S curates next route, likely implementation acceptance review if C2-I lands
```

If C1-A holds:

```text
do not run C2-I
route exact proof/design blockers
preserve current OOF-TY0 unsupported boundary
```

If C2-I lands:

```text
implementation still requires a later acceptance decision unless C1-A
explicitly authorizes same-round closure criteria and C3-S can only curate,
not accept architecture
```
