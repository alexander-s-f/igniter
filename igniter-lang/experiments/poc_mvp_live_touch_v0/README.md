# POC MVP Live Touch v0

Local-only proof lab for S3-R157-C2-I.

Run:

```text
ruby igniter-lang/experiments/poc_mvp_live_touch_v0/poc_mvp_live_touch_v0.rb
```

The lab compiles exactly four independent synthetic `.ig` modules through the
existing Igniter-Lang compile surface and writes proof-local outputs under
`out/`.

This is not a public demo, release claim, production runtime, or language
semantics route.
