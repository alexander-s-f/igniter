# Temporal Contracts v1

Historical reference.

For the current canonical reading, start with:

- [docs/guide/core-runtime-features.md](./guide/core-runtime-features.md)
- [docs/core/README.md](./core/README.md)

## What This Old Document Was About

The V1 write-up described making time an explicit input so historical execution
could be replayed reproducibly.

## What Is Still Historically Useful

- the original `as_of` mental model
- the historical `temporal_compute` framing
- the relation between temporal replay and content-addressed reuse

## What Changed Since V1

Temporal support now belongs under focused core-runtime features instead of
standing as a large top-level entrypoint on its own.
